services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: tea_tracker
      POSTGRES_USER: tea_tracker
      POSTGRES_PASSWORD: tea_tracker
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: